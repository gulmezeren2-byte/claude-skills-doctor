---
name: tailwind-design
description: "Utility composition without a class-name soup: extracting a component when a pattern repeats, using the scale instead of arbitrary values, and keeping dark mode a variant rather than a fork."
---

# tailwind-design

Utility composition without a class-name soup: extracting a component when a
pattern repeats, using the scale instead of arbitrary values, and keeping dark
mode a variant rather than a fork.
