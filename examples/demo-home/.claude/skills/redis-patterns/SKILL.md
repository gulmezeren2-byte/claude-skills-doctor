---
name: redis-patterns
description: "Caching, locks and queues on Redis: TTL discipline, the lock that expires mid-work, and why a cache stampede shows up only under load."
---

# redis-patterns

Caching, locks and queues on Redis: TTL discipline, the lock that expires mid-
work, and why a cache stampede shows up only under load.
