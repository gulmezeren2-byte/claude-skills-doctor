---
name: perf-profiling
description: "Find where the time actually goes before optimising: sampling versus instrumentation, reading a flame graph, and separating a slow algorithm from a slow I/O wait."
---

# perf-profiling

Find where the time actually goes before optimising: sampling versus
instrumentation, reading a flame graph, and separating a slow algorithm from a
slow I/O wait.
