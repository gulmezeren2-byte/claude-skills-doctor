---
name: rust-ownership
description: "Borrow checker fluency: lifetimes that actually need naming, when to reach for Rc/RefCell, and the clone that is fine versus the clone that hides a design problem."
---

# rust-ownership

Borrow checker fluency: lifetimes that actually need naming, when to reach for
Rc/RefCell, and the clone that is fine versus the clone that hides a design
problem.
