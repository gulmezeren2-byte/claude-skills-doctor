---
name: test-strategy
description: "Choose the cheapest test that would actually have caught the bug: what belongs in a unit test versus an integration test, and when a test asserts the implementation instead of the behaviour."
---

# test-strategy

Choose the cheapest test that would actually have caught the bug: what belongs
in a unit test versus an integration test, and when a test asserts the
implementation instead of the behaviour.
