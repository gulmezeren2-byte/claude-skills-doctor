---
name: typescript-types
description: "Model the domain in the type system: discriminated unions over optional soup, when a generic earns its complexity, and reading a long inference error."
---

# typescript-types

Model the domain in the type system: discriminated unions over optional soup,
when a generic earns its complexity, and reading a long inference error.
