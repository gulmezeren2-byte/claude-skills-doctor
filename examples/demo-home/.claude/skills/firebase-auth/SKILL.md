---
name: firebase-auth
description: "Sign-in flows, custom claims and token refresh: what belongs in a claim versus a database read, and the race between a claim change and the token that still carries the old one."
---

# firebase-auth

Sign-in flows, custom claims and token refresh: what belongs in a claim versus
a database read, and the race between a claim change and the token that still
carries the old one.
