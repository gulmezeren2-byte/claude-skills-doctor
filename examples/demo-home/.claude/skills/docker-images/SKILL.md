---
name: docker-images
description: "Build small, reproducible images: layer ordering for cache hits, multi-stage builds, pinning by digest, and why running as root is still the most common finding."
---

# docker-images

Build small, reproducible images: layer ordering for cache hits, multi-stage
builds, pinning by digest, and why running as root is still the most common
finding.
