---
name: mcp-integration
description: "Wire an MCP server into a client: transports, tool schemas that an agent can route on, and the read-only guarantees worth enforcing in code."
---

# mcp-integration

Wire an MCP server into a client: transports, tool schemas that an agent can
route on, and the read-only guarantees worth enforcing in code.
