---
name: nextjs-app-router
description: "Routing, layouts, streaming and cache semantics in the Next.js App Router: which boundary makes a component a server component, why a fetch is deduplicated, when revalidation actually happens, and reading the build output to see what became static."
---

# nextjs-app-router

Routing, layouts, streaming and cache semantics in the Next.js App Router:
which boundary makes a component a server component, why a fetch is
deduplicated, when revalidation actually happens, and reading the build output
to see what became static.
